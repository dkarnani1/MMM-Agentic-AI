# ─────────────────────────────────────────────────────────────────────────────
# Marketing Mix Model AI POC — Python Dependencies
# Install: pip install -r requirements.txt
# Python:  >= 3.10
# ─────────────────────────────────────────────────────────────────────────────

# Core data stack
pandas>=2.0.0
numpy>=1.24.0

# Statistical modelling
statsmodels>=0.14.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0          # Interactive charts (Streamlit UI)

# Terminal UI
rich>=13.0.0

# Web UI
streamlit>=1.32.0

# Claude API (optional — for advanced NLP routing)
anthropic>=0.40.0

# Utilities
python-dotenv>=1.0.0    # Load keys from .env
tabulate>=0.9.0         # Fallback table formatting

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# ─────────────────────────────────────────────────────────────────────────────
# Live Data Connectors (optional — install only if using real API data)
# ─────────────────────────────────────────────────────────────────────────────
# google-ads>=24.0.0               # Google Ads connector
# facebook-business>=19.0.0        # Meta / Facebook Ads connector
# google-analytics-data>=0.18.0    # Google Analytics 4 connector
